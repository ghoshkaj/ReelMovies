'use strict';

describe('myApp.movieDetailsView module', function() {

  beforeEach(module('myApp.movieDetailsView'));

  describe('movieDetailsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});