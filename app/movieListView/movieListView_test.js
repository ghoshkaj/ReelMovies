'use strict';

describe('myApp.movieListView module', function() {

  beforeEach(module('myApp.movieListView'));

  describe('movieListView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});